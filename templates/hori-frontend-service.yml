apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-frontend-service"
spec:
  selector:
    app: hori-frontend
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 32000
